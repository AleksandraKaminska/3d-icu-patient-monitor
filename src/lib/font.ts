/**
 * Local font for drei's <Text> so the on-screen labels don't fetch Roboto from
 * a CDN at runtime (fully offline / self-contained). WOFF 1.0 - the format
 * troika-three-text can parse (not WOFF2).
 */
export const SCREEN_FONT = '/fonts/inter-medium.woff'
